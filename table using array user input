#include <stdio.h>
int main() 
{
    int a[10],i,n;
    printf("table of:");scanf("%d",&n);
    for (i=1;i<=10;i++){
        a[i]=n*i;
    }
    for (i=1;i<=10;i++){
        printf("%d x %d = %d\n",n,i,a[i]);
    }
}
